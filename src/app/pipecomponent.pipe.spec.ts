import { PipecomponentPipe } from './pipecomponent.pipe';

describe('PipecomponentPipe', () => {
  it('create an instance', () => {
    const pipe = new PipecomponentPipe();
    expect(pipe).toBeTruthy();
  });
});
